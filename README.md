# ImbalanceCalc

Розрахунок платежу за небаланси електричної енергії для учасника балансуючої
групи гарантованого покупця.

Користувач завантажує місячний файл ГП через веб-інтерфейс, дані
перераховуються за Порядком здійснення розрахунків БГ ГП (Додаток 2 до Типового
договору), після чого формується:

- **місячний підсумок** — платіж без ПДВ, ПДВ і всього з ПДВ;
- **обмеження ОСП** — всього обмеження (год.хв) та обмежений виробіток (МВт·год),
  по добах і за місяць;
- **розрахунок по добах** — обсяги, відхилення та платіж за кожну добу;
- **окремий аналіз діб**, платіж за які перевищує **1 000 грн** (поріг налаштовується);
- **порівняння з іншим місяцем** — за замовчуванням з попереднім;
- **PDF-звіт** із вибором теки збереження (типово системна тека «Завантаження»).

Методика описана окремим самодостатнім документом:
[docs/METHODOLOGY.md](docs/METHODOLOGY.md) — його можна використати як
специфікацію в іншому проєкті.

Інструкція для користувача: [docs/user-guide.docx](docs/user-guide.docx) —
встановлення та робота з програмою мовою без технічних термінів. Генерується
командою `python scripts/make_user_guide.py`, щоб версія та значення
параметрів не розходилися з кодом.

Windows-додаток: [docs/WINDOWS_APP_PLAN.md](docs/WINDOWS_APP_PLAN.md) —
план і поточний стан.

## Для користувачів

Готовий інсталятор — на сторінці
[Releases](https://github.com/IgorParf/ImbalanceCalc/releases). Python
встановлювати не потрібно. Покрокова інструкція, включно з обходом попередження
SmartScreen, — [docs/user-guide.docx](docs/user-guide.docx).

## Встановлення для розробки

```bash
python -m venv venv
venv\Scripts\activate          # Windows
pip install -e ".[dev]"
```

## Запуск

У браузері:

```bash
streamlit run app.py
```

У нативному вікні Windows (потрібен `pip install -e ".[desktop]"`):

```bash
python desktop/main.py
```

Сторінки задаються в [app.py](app.py) через `st.navigation`, тому імена файлів
англійські, а в меню — українські назви:

| Сторінка | Файл | Що містить |
|---|---|---|
| **Головна** | [views/home.py](views/home.py) | вибір періоду або завантаження файлу, місячний підсумок з ПДВ та обмеженнями, розрахунок по добах, погодинна деталізація, вивантаження звіту |
| **Аналіз** | [views/analysis.py](views/analysis.py) | загальний аналіз (концентрація платежу, розподіл за годинами, карта «доба × година», найдорожчі години), аналіз по добах з розбором обраної доби, порівняння з іншим місяцем |

Консольний варіант:

```bash
imbalance-calc "data/input/<EIC-код> липень 2026.xlsx" --pdf
```

Тести:

```bash
pytest
```

Синтетичний зразок вхідного файлу (реальні дані в репозиторій не потрапляють):

```bash
python scripts/make_sample.py
```

Звірка з реальним рахунком: покладіть місячний файл ГП у `data/input/`,
виконайте `python scripts/make_control_config.py` і впишіть у створений
`data/input/control.json` суму без ПДВ із рахунку. Після цього вмикається
`tests/test_control_example.py`. Ані файл, ані `control.json` не
відстежуються git — реальні дані комерційного обліку та суми рахунків у
репозиторій не потрапляють.

## Сховище даних

Файл достатньо завантажити **один раз**: після імпорту розібрані погодинні дані
лежать у `data/store/<період>.parquet`, а перелік періодів — у
`data/store/index.json`. При наступному відкритті застосунку період обирається
зі списку, файл не потрібен.

| Крок | Час |
|---|---|
| читання вихідного xlsx | ~253 мс |
| читання з parquet | ~7 мс |
| розрахунок | ~45 мс |

У сховищі зберігаються **розібрані вхідні дані, а не готові результати**: це
дозволяє змінювати параметри (`K_e`, `α`, `K_im`, ПДВ, поріг) і одразу бачити
перерахунок, без ризику показати застарілі числа. Повторне завантаження того
самого файлу розпізнається за sha256 і не спричиняє повторного читання.

У запакованому додатку дані користувача переносяться у
`%LOCALAPPDATA%\ImbalanceCalc` — каталог встановлення доступний лише для
читання. Каталог сховища перевизначається змінною `IC_STORE_DIR`, каталог
даних цілком — змінною `IC_DATA_DIR`.
Керування періодами (перелік, видалення) — у блоці «Сховище даних» на головній
сторінці.

## Збірка Windows-додатку

```powershell
pip install -e ".[build]"
.\installer\build.ps1 -Clean -Installer
```

Скрипт сам готує іконку та ресурс версії, збирає застосунок і — якщо знайде
`ISCC.exe` — інсталятор. Зібрати лише інсталятор з готової теки:
`.\installer\build.ps1 -SkipApp -Installer`.

| Артефакт | Розмір |
|---|---|
| `dist\ImbalanceCalc\` (застосунок) | 230 МБ |
| `dist\ImbalanceCalc-0.1.0-setup.exe` | 70 МБ |
| після встановлення | 235 МБ |

| Файл | Призначення |
|---|---|
| [desktop/main.py](desktop/main.py) | піднімає Streamlit на `127.0.0.1` і показує його вікном pywebview |
| [installer/imbalance_calc.spec](installer/imbalance_calc.spec) | специфікація PyInstaller (one-folder) |
| [installer/build.ps1](installer/build.ps1) | збірка застосунку та інсталятора однією командою |
| [installer/setup.iss](installer/setup.iss) | інсталятор Inno Setup 6 |
| [scripts/make_icon.py](scripts/make_icon.py) | генерація `installer/app.ico` |
| [scripts/make_version_info.py](scripts/make_version_info.py) | ресурс версії для `.exe` |

Встановлення відбувається в `%LOCALAPPDATA%\Programs\ImbalanceCalc` — без прав
адміністратора, тому UAC не з'являється ані при встановленні, ані при оновленні.
Дані користувача (сховище періодів, логи) — у `%LOCALAPPDATA%\ImbalanceCalc`;
деінсталятор їх не чіпає.

Подробиці та відкинуті альтернативи — [docs/WINDOWS_APP_PLAN.md](docs/WINDOWS_APP_PLAN.md).

## Куди зберігаються звіти

За замовчуванням — у системну теку **«Завантаження»**. Шлях беремо з
`SHGetKnownFolderPath`, а не складаємо як `~/Downloads`: користувач міг
перенести теку на інший диск.

| Режим | Поведінка |
|---|---|
| нативне вікно | після формування відкривається системний діалог «Зберегти як», стартова тека — «Завантаження»; після збереження є кнопка «Показати в Провіднику» |
| браузер | нативний діалог показати неможливо (сторінка виконується на боці клієнта), тому файл віддається через `st.download_button` — браузер кладе його у свою теку завантажень або показує власний діалог, якщо ввімкнено «Завжди питати, куди зберігати файли» |
| CLI | `--pdf` / `--excel` пишуть у теку «Завантаження»; інша тека — прапорцем `-o` |

Теку за замовчуванням можна перевизначити змінною `IC_REPORTS_DIR`.

## Вхідні дані

Місячний файл гарантованого покупця (xlsx) з 15 аркушами однакового формату:
рядок 1 — заголовок (`Доба`, `Зона`, `1 год` … `24 год`), далі по рядку на добу.
Перелік аркушів і відповідність величинам методики — розділ 8
[docs/METHODOLOGY.md](docs/METHODOLOGY.md).

Файл містить і вихідні, і похідні величини, тому при завантаженні звіряються
контрольні співвідношення (наприклад, `Фактичне відхилення = Факт − Прогноз + ΔW`);
розбіжності показуються як зауваження, розрахунок при цьому не зупиняється.

## Параметри розрахунку

Задаються у бічній панелі UI (або прапорцями CLI), бо у файлі ГП їх немає:

| Параметр | Опис | За замовчуванням |
|---|---|---|
| `K_e` | допустиме відхилення для типу генерації ВДЕ, % | 5 |
| `α` | частка відшкодування вартості врегулювання, % | 100 |
| `K_im` | коефіцієнт ціни небалансу за Правилами ринку | 0,05 |
| ПДВ | ставка податку, % | 20 |
| поріг доби | сума, вище якої доба аналізується окремо, грн | 1 000 |

> Набір значень за замовчуванням звірений з реально виставленим рахунком:
> розрахунок збігся із сумою у рахунку до копійки (розбіжність — округлення
> рахунку). Самі суми в репозиторії не публікуються, методика звірки описана
> в розділі 13 [docs/METHODOLOGY.md](docs/METHODOLOGY.md).
> При зміні редакції нормативних актів значення треба перевіряти повторно;
> найчутливіший параметр — `K_im`.

## Обмеження ОСП

Файл ГП подає **обсяг** невідпущеної енергії (аркуш `ΔW`), а не час дії команди.
Тому тривалість рахується як еквівалентна: у кожній годині береться частка
`ΔW / (факт + ΔW)`, і ці частки підсумовуються. Наприклад, `ΔW = 4,120` при
фактичних `1,3842` МВт·год означає потенціал `5,504` МВт·год і `0,749` год =
45 хв обмеження.

Показники обмежень суто інформативні — у розрахунок платежу вони входять лише
через саму величину `ΔW` у формулах методики.

## Структура

```
app.py                       точка входу Streamlit (навігація)
views/home.py                сторінка «Головна»
views/analysis.py            сторінка «Аналіз»
scripts/make_sample.py       генератор синтетичного вхідного файлу
src/imbalance_calc/
    config.py                параметри розрахунку та шляхи
    models.py                SettlementResult: підсумки, ПДВ, обмеження, доби понад поріг
    store.py                 сховище періодів у parquet
    exceptions.py            винятки пакета
    cli.py                   консольний інтерфейс
    dataio/
        schema.py            відповідність аркушів файлу величинам методики
        loaders.py           xlsx -> погодинна таблиця
        validators.py        контрольні співвідношення та повнота даних
    core/
        methodology.py       глави 1-3 Порядку: вартість, W^alpha, частка Учасника
        settlement.py        збірка результату, коригуючий платіж
        daily.py             добові підсумки та поріг за добу
    reporting/
        summary.py           таблиці, форматування, порівняння місяців
        pdf_report.py        PDF-звіт (reportlab + діаграма matplotlib)
        excel_report.py      вивантаження в xlsx
    ui/components.py         спільні елементи інтерфейсу
    ui/save_dialog.py        системний діалог збереження звіту
desktop/main.py              запуск у нативному вікні (pywebview)
installer/imbalance_calc.spec  специфікація PyInstaller
installer/build.ps1          скрипт збірки
tests/                       108 тестів: формули, завантаження, сховище, обмеження,
                             шляхи, діалог збереження, звіти, сторінки UI,
                             звірка з виставленим рахунком
data/input | output | samples | store
docs/METHODOLOGY.md          опис методики
```

## Ліцензія

[MIT](LICENSE).

## Перевірка результату

Платіж за годину `CIEQ_p,t` завжди невід'ємний: це сума до сплати Учасником, а
не сальдо. Години, у яких відносне відхилення не перевищує `K_e`, не
тарифікуються взагалі, тому кількість «годин з платежем» зазвичай суттєво менша
за 744.
