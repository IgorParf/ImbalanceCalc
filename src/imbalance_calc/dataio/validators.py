"""Перевірка повноти та коректності вхідних даних."""

from __future__ import annotations

import pandas as pd


def validate_dataframe(df: pd.DataFrame) -> list[str]:
    """Перевірити таблицю та повернути список попереджень.

    Що перевіряємо:
      * наявність обов'язкових колонок;
      * пропуски у часовому ряді (відсутні години/квартагодини);
      * дублікати міток часу;
      * від'ємні або нечислові обсяги;
      * перехід на літній/зимовий час (доба на 23 та 25 годин).

    TODO: реалізувати перевірки.
    """
    raise NotImplementedError
